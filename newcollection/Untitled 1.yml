info:
  name: Untitled 1
  type: http
  seq: 2

http:
  method: GET
  url: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        pm.test("Status code is 200", function () {
            pm.response.to.have.status(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
